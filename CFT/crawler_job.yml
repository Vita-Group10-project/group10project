AWSTemplateFormatVersion: "2010-09-09"
Description: Glue Database + Crawler for Enriched (Silver) Layer


# ==================================================
# PARAMETERS
# ==================================================
Parameters:

  DatabaseName:
    Type: String
    Default: enrich
    Description: Athena/Glue database name

  TargetPath:
    Type: String
    Default: s3://enriched-zone-00/transformed/data/
    Description: S3 path to enriched transformed data

  TablePrefix:
    Type: String
    Default: enrich_
    Description: Prefix for generated Glue tables

  CrawlerName:
    Type: String
    Default: enrich-transformed-crawler
    Description: Name of Glue crawler

  GlueRoleName:
    Type: String
    Default: glue-execution-role
    Description: Existing Glue IAM Role name (NOT ARN)


# ==================================================
# RESOURCES
# ==================================================
Resources:


  # ==================================================
  # GLUE DATABASE
  # ==================================================
  EnrichDatabase:
    Type: AWS::Glue::Database
    Properties:
      CatalogId: !Ref AWS::AccountId
      DatabaseInput:
        Name: !Ref DatabaseName
        Description: Enriched/Silver analytics layer tables


  # ==================================================
  # GLUE CRAWLER
  # ==================================================
  EnrichCrawler:
    Type: AWS::Glue::Crawler
    Properties:
      Name: !Ref CrawlerName

      Role: !Sub arn:aws:iam::${AWS::AccountId}:role/${GlueRoleName}

      DatabaseName: !Ref DatabaseName

      Targets:
        S3Targets:
          - Path: !Ref TargetPath

      TablePrefix: !Ref TablePrefix


      # ✅ FIXED SETTINGS (Glue required combo)
      SchemaChangePolicy:
        UpdateBehavior: LOG
        DeleteBehavior: LOG

      RecrawlPolicy:
        RecrawlBehavior: CRAWL_NEW_FOLDERS_ONLY

      Configuration: |
        {
          "Version": 1.0,
          "Grouping": {
            "TableGroupingPolicy": "CombineCompatibleSchemas"
          }
        }


# ==================================================
# OUTPUTS
# ==================================================
Outputs:

  DatabaseName:
    Value: !Ref DatabaseName

  CrawlerName:
    Value: !Ref CrawlerName

  CrawlPath:
    Value: !Ref TargetPath
